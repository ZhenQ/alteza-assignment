{% extends 'base_layout.twig' %}

{% block content %}
    <dl>
        {% for article in articles %}
            <dd class="text-xl font-bold">{{ article.title }} </dd>
            <dt class="mb-5">{{ article.body }}</dt>
        {% endfor %}
    </dl>
{% endblock %}