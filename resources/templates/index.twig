{% extends 'base_layout.twig' %}

{% block content %}
    <dl>
        {% for article in articles %}
            <dd>{{ article.title }} </dd>
            <dt>{{ article.body }}</dt>
        {% endfor %}
    </dl>
{% endblock %}